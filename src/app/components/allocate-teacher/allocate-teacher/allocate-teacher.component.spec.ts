/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AllocateTeacherComponent } from './allocate-teacher.component';

describe('AllocateTeacherComponent', () => {
  let component: AllocateTeacherComponent;
  let fixture: ComponentFixture<AllocateTeacherComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AllocateTeacherComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AllocateTeacherComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
