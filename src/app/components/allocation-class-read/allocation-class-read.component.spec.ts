/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AllocationClassReadComponent } from './allocation-class-read.component';

describe('AllocationClassReadComponent', () => {
  let component: AllocationClassReadComponent;
  let fixture: ComponentFixture<AllocationClassReadComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AllocationClassReadComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AllocationClassReadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
